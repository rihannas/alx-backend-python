apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: ClusterIP
  selector:
    app: django
    version: blue # start with blue
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
